name: feature_home
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  shared_ui:
    path: ../shared_ui
